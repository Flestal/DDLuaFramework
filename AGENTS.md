# DDLuaFramework 작업 지침

- DDLuaFramework의 브리지 관련 기능을 추가하거나 수정할 때, 하위 primitive만으로 Lua에서 안전하게 동일한 기능을 재현할 수 있다면 그 조합 기능은 네이티브 필수 primitive가 아니다.
- C++ 브리지는 게임 버전별 주소, 네이티브 ABI, 메모리 레이아웃 검증, 객체 수명, 실행 시점, 동시성처럼 Lua가 안전하게 책임질 수 없는 경계만 소유한다.
- 대상 선택, 그룹 순회, 필터링, 제외 조건, 개수 제한, 효과 이름과 수치, 여러 primitive의 조합은 Lua에 둔다.
- 반복되는 조합이 필요하면 먼저 Lua 공용 함수나 라이브러리로 제공한다. 편의성만을 이유로 C++ 복합 API를 추가하지 않는다.
- Lua 재현이 수명·원자성·성능·안전성을 실제로 해칠 때만 네이티브 복합 기능을 예외로 허용하며, 그 이유와 더 작은 primitive로 분해할 수 없는 이유를 문서화한다.
- Lua에는 원시 포인터나 임의 네이티브 호출 수단을 노출하지 않는다. 네이티브 객체 참조는 검증된 제한 API만 제공하고 유효 범위를 벗어나면 닫힌 상태로 실패해야 한다.
